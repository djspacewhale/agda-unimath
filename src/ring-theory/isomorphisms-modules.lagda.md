# Isomorphisms of (left/right) `R`-modules

```agda
module ring-theory.isomorphisms-modules where
```

<details><summary>Imports</summary>

```agda
open import foundation.structure-identity-principle

open import ring-theory.modules-rings
open import ring-theory.homomorphisms-modules
```

We characterize the identity types of `left-module-Ring l R` using the
[structure identity principle](foundation.structure-identity-principle.md) and
likewise for right modules.
